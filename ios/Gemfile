source 'https://rubygems.org'

# You may use http://rbenv.org/ or https://rvm.io/ to install and use this version
ruby ">= 2.6.10"

# Cocoapods 1.15 is required to build React Native's iOS Targets.
gem 'cocoapods', '~> 1.15'
gem 'activesupport', '>= 6.1.7.5', '!= 7.1.0'

# fastlane drives the signed TestFlight release (see fastlane/Fastfile). Run locally with
# `bundle exec fastlane <lane>`; CI uses the same Gemfile via bundler.
gem 'fastlane'
